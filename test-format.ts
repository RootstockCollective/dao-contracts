// Плохо отформатированный файл для тестирования
const badlyFormatted = {
  foo: 'bar',
  baz: 'qux',
}

function testFunction(param1: string, param2: number) {
  return {
    result: param1 + param2,
  }
}

export { badlyFormatted, testFunction }
